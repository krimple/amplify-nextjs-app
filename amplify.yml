version: 1
frontend:
  phases:
    preBuild:
      commands:
        - amplifyPush --simple
        - npm ci
    build:
      commands:
        - ls -lR .
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
